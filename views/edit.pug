doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title edit information
    link(rel='stylesheet' href="edit.css")

    body 
        #editForm 
            form(id='edit-form' name='edit-form' method='post' action='/edit/' + question._id)
                p 1) What animal does not appear on the Chinese Zodiac?
                    br
                    //Bull is correct
                    select(name='mult1' value=question.question1)
                        option(selected=question.question1) #{question.question1}  
                        option(value='Dragon') Dragon
                        option(value='Rabbit') Rabbit 
                        option(value='Dog') Dog 
                        option(value='Bull') Bull
                br
                p 2) How many hearts does an octopus have?
                    br
                    //3 is correct
                    select(name='mult2' value=question.question3)
                        option(selected=question.question3) #{question.question3} 
                        option(value=1) 1
                        option(value=2) 2
                        option(value=3) 3 
                        option(value=4) 4
                
                br
                p 3) What are the 3 distinct colors of the powerpuff girls?
                    br
                    //Blue, Red, Green  is correct
                    select(name='mult3' value=question.question4)
                        option(selected=question.question4) #{question.question4} 
                        option(value='Green, Purple, Orange') Green, Purple, Orange 
                        option(value='Red, Purple, Blue') Red, Purple, Blue 
                        option(value='Green, Purple, Orange') Blue, Red, Green 
                        option(value='Green, Purple, Orange') Pink, Blue, Green 
                input(type='submit' value='submit')
    form(method='get' action='http://localhost:3000/logout')
        input(type='submit' value='logout')